<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/rider_splash_screen"
    android:padding="@dimen/dp_16">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="@dimen/dp_16"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="@dimen/dp_16">

        <Button
            android:id="@+id/btn_phone_sign_in"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/dp_10"
            android:background="@color/white"
            android:drawableStart="@drawable/ic_baseline_phone_24"
            android:padding="@dimen/dp_16"
            android:text="@string/sign_in_with_phone"
            android:textColor="@color/black" />

        <Button
            android:id="@+id/btn_goole_sign_in"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:background="@color/white"
            android:drawableStart="@drawable/fui_ic_googleg_color_24dp"
            android:padding="@dimen/dp_16"
            android:text="@string/sign_in_with_goole"
            android:textColor="@color/black" />
    </LinearLayout>
</RelativeLayout>